import 'package:flutter/material.dart';

class OrderPage extends StatelessWidget {
  final int? supplierId;
  const OrderPage({super.key, this.supplierId});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
